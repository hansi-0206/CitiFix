import React from "react";

// The Heatmap is rendered natively inside MapView.jsx using React Leaflet Markers.
// This component remains as a clean stub to prevent import failures.
export default function HeatmapView() {
  return null;
}
